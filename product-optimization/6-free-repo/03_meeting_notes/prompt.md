Read the meeting transcript below. Extract:

1. **Decisions made** — bullet list, one line each, no commentary
2. **Action items** — owner, action, deadline (if mentioned)
3. **Open questions** — things left unresolved
4. **Suggested follow-up date** — based on the conversation

Rules:
- Ignore small talk and tangents
- If something is ambiguous, mark it `AMBIGUOUS — needs clarification`
  rather than guessing. This is the most important rule.
- If a deadline isn't stated, write `(no deadline)` — do not invent one
- If you're not sure who owns an action item, write `OWNER UNCLEAR`

Output format: clean markdown with the four sections above as `##` headings.
No preamble, no closing remarks, just the structured output.

Transcript:

{transcript}
